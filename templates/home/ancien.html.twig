{% extends 'base.html.twig' %}

{% block title %} Accueil - Douceurs & Snacks de Leila {% endblock %}

{% block body %}

<section class="text-center py-5 bg-pink-100">
    <h1 class="text-3xl font-cursive">Douceurs & snacks de Leila</h1>
    <p class="text-pink-800">Des saveurs de fait maison pour tous les moments – faites avec amour et une touche spéciale de Leila.</p>
</section>

{# <section class="container mx-auto py-10">
    <h2 class="text-2xl font-bold text-center mb-6">Nos Produits Snacks</h2>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
       
        <div class="bg-white rounded shadow p-4 text-center">
            <img src="/images/snack1.jpg" alt="Bolinho de Bacalhau" class="mx-auto rounded mb-2">
            <h3 class="font-bold">Bolinho de Bacalhau</h3>
            <p class="text-sm">Description du produit ici...</p>
            <p class="text-green-600 font-semibold mt-2">0.90 € / unité</p>
            <input type="number" value="1" class="border rounded w-16 text-center mx-auto mt-2">
            <button class="bg-pink-600 text-white rounded mt-2 px-4 py-1">Commander</button>
        </div>

    </div>
</section> #}

<section class="container mx-auto py-10">
    <h2 class="text-2xl font-bold text-center mb-6">Nos Produits Snacks</h2>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
          {% for product in Snacks %}
            <div class="bg-white rounded shadow p-4 text-center hover:shadow-lg transition">
            <img class="mx-auto rounded mb-2 transform hover:scale-105 transition duration-300">
                <img src="/images/{{ product.image }}" alt="Image du produit {{ product.titre }}">
                <img src="/images/{{ product.image }}" alt="{{ product.titre }}" class="mx-auto rounded mb-2">
                <h3 class="font-bold">{{ product.titre }}</h3>
                <p class="text-sm">{{ product.description|length > 100 ? product.description[:100] ~ '...' : product.description }}</p>
                {% if product.urlvideo %}
                    <a href="{{ product.urlvideo }}" target="_blank" class="text-blue-500 underline block mt-2">Voir en vidéo</a>
                {% endif %}
              <a href="{{ path('product_show', {'id': product.id}) }}">
                    <button class="bg-pink-600 text-white rounded mt-2 px-4 py-1">Commander</button>
            </a>

                <p class="text-green-600 font-semibold mt-2">{{ (product.prix / 100)|number_format(2, ',', ' ') }} € / unité</p>
                <input type="number" value="1" class="border rounded w-16 text-center mx-auto mt-2">
                <button class="bg-pink-600 text-white rounded mt-2 px-4 py-1">Commander</button>
            </div>
        {% else %}
            <p>Aucun snack disponible pour le moment.</p>
        {% endfor %}
    </div>
</section>

<section class="container mx-auto py-10">
    <h2 class="text-2xl font-bold text-center mb-6">Nos Produits Douceurs</h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        {% for product in Douceurs %}
            <div class="bg-white rounded shadow p-4 text-center">
                <img src="/images/{{ product.image }}" alt="{{ product.titre }}" class="mx-auto rounded mb-2">
                <h3 class="font-bold">{{ product.titre }}</h3>
                <p class="text-sm">{{ product.description|length > 100 ? product.description[:100] ~ '...' : product.description }}</p>
                <p class="text-green-600 font-semibold mt-2">{{ (product.prix / 100)|number_format(2, ',', ' ') }} € / unité</p>
                <input type="number" value="1" class="border rounded w-16 text-center mx-auto mt-2">
               <a href="{{ path('product_show', {'id': product.id}) }}">
                    <button class="bg-pink-600 text-white rounded mt-2 px-4 py-1">Commander</button>
                </a>
            </div>
        {% else %}
            <p>Aucune douceur disponible pour le moment.</p>
        {% endfor %}
    </div>
</section>

{# {% for product in snacks %}
    {{ product.titre }} - {{ product.prix }} €
{% endfor %} #}



{% endblock %}
