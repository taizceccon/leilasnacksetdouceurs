{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    {% for flash_error in app.flashes('verify_email_error') %}
        <div class="alert alert-danger bg-red-100 text-red-700 p-4 mb-6 rounded text-center">
            {{ flash_error }}
        </div>
    {% endfor %}

    <section class="banner-contact bg-pink-100 py-12 text-center rounded-md mb-10">
        <h1 class="text-4xl font-cursive text-pink-700 mb-4">Inscrivez-vous</h1>
        <p class="text-pink-800 max-w-2xl mx-auto text-lg">
            Créez votre compte pour accéder à toutes nos douceurs personnalisées 🍰<br>
            C’est rapide, facile et surtout délicieux !
        </p>
    </section>

    <div class="contact-form max-w-lg mx-auto bg-white p-8 rounded shadow">
        <h2 class="text-2xl font-bold text-center mb-6 text-pink-700">Créer un compte</h2>

        {{ form_start(registrationForm) }}

        <div class="form-group mb-4">
            {{ form_label(registrationForm.email, null, {'label_attr': {'class': 'font-semibold'}}) }}
            {{ form_widget(registrationForm.email, {'attr': {'class': 'border border-pink-300 rounded w-full p-2'}}) }}
            {{ form_errors(registrationForm.email) }}
        </div>

        <div class="form-group mb-4">
            {{ form_label(registrationForm.plainPassword, 'Mot de passe', {'label_attr': {'class': 'font-semibold'}}) }}
            {{ form_widget(registrationForm.plainPassword, {'attr': {'class': 'border border-pink-300 rounded w-full p-2'}}) }}
            {{ form_errors(registrationForm.plainPassword) }}
        </div>

        <div class="form-group mb-6">
            {{ form_widget(registrationForm.agreeTerms, {'attr': {'class': 'mr-2'}}) }}
            {{ form_label(registrationForm.agreeTerms, null, {'label_attr': {'class': 'inline font-medium'}}) }}
            {{ form_errors(registrationForm.agreeTerms) }}
        </div>

        <div class="form-group text-center">
            <button type="submit" class="bg-pink-600 hover:bg-pink-700 text-white px-6 py-2 rounded cursor-pointer">
                🍬 S'inscrire
            </button>
        </div>

        {{ form_end(registrationForm) }}
    </div>
{% endblock %}